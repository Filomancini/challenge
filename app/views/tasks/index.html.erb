<h1>My task!</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <i class="fas fa-check"></i>
      <%= link_to task.title, task_path(task) %>
      <%= link_to "delete", task_path(task.id), meyhod: :delete, data:
      {confirm: "delete #{task.title}"} %>
    </li>
  <% end %>
</ul>
